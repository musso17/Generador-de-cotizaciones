
import { ServiceType, ClientIndustry } from './types';

export const IGV_RATE = 0.18;

export const SERVICE_TYPES: ServiceType[] = Object.values(ServiceType);
export const CLIENT_INDUSTRIES: ClientIndustry[] = Object.values(ClientIndustry);
